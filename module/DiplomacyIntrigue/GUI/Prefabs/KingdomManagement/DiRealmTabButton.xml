<Prefab>
  <Constants>
    <Constant Name="Header.Tab.Right.Width" BrushLayer="Default" BrushName="Header.Tab.Right" BrushValueType="Width" />
    <Constant Name="Header.Tab.Right.Height" BrushLayer="Default" BrushName="Header.Tab.Right" BrushValueType="Height" />
    <Constant Name="Header.Tab.Right.Width.Scaled" MultiplyResult="0.90" Value="!Header.Tab.Right.Width" />
    <Constant Name="Header.Tab.Right.Height.Scaled" MultiplyResult="0.90" Value="!Header.Tab.Right.Height" />
  </Constants>
  <Window>
    <!-- Sixth header tab. The compiled KingdomTabControlListPanel only knows the five
         vanilla button/panel pairs, so this button manages itself: IsSelected binds to
         our panel's Show the way the tab control computes it for the vanilla buttons,
         and ExecuteShow hides the five vanilla categories itself. DataSource narrows
         the context from KingdomManagementVM to our realm VM. -->
    <ButtonWidget DataSource="{DiRealm}" DoNotPassEventsToChildren="true"
        WidthSizePolicy="Fixed" HeightSizePolicy="Fixed"
        SuggestedWidth="!Header.Tab.Right.Width.Scaled" SuggestedHeight="!Header.Tab.Right.Height.Scaled"
        HorizontalAlignment="Right" VerticalAlignment="Center" Brush="Header.Tab.Right"
        Command.Click="ExecuteShow" IsSelected="@Show" IsVisible="@TabVisible"
        UpdateChildrenStates="true">
      <Children>
        <TextWidget WidthSizePolicy="CoverChildren" HeightSizePolicy="CoverChildren"
            HorizontalAlignment="Center" VerticalAlignment="Center" PositionXOffset="-10"
            MarginTop="3" Brush="Clan.TabControl.Text" Text="@TabText" />
      </Children>
    </ButtonWidget>
  </Window>
</Prefab>
