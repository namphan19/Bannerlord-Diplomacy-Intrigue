# Build output
[Bb]in/
[Oo]bj/
*.user
*.suo
.vs/
.idea/
*.dll
*.pdb

# Keep the module staging tree, but never commit compiled artifacts
!module/**/*.xml

# Tooling
tools/**/bin/
tools/**/obj/
artifacts/
*.log

# Diagnostic tool output
artifacts/

# Archived balance runs are data, not build noise
!docs/balance/*.log

# Node tooling dependencies (the bridges)
node_modules/

# opencode local config (each clone has its own)
opencode.jsonc
